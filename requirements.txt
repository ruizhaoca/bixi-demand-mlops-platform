# Streamlit serving dependencies (app.py on Community Cloud, app_ec2.py on EC2).
# Pinned to a proven, mutually-compatible generation so Streamlit Community Cloud
# builds are reproducible. Unpinned ">=" previously let the Cloud resolve to
# bleeding-edge majors (numpy 2.x, pandas 3.x) and fail at import; these upper
# bounds lock to the stable generation the committed model artifacts load under.
streamlit>=1.44,<2
pandas>=2.2,<2.3
numpy>=1.26,<2
pyarrow>=15,<18
requests>=2.31,<3
plotly>=5.20,<6
scikit-learn>=1.4,<1.6
lightgbm>=4.3,<4.6
boto3>=1.34,<2
